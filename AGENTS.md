# Repository Guidelines

## 프로젝트 구조 및 모듈 구성
- 루트는 대회/과제별 워크스페이스를 담습니다. 각 챌린지는 자신의 폴더(`dacon/`, `kaggle/`) 안에서 노트북과 보조 스크립트를 관리하세요.  
- 공용 데이터셋은 `data/`, `data1/`에, 결과물과 제출 파일은 `result/`에 둡니다. 원본 데이터와 코드를 같은 디렉터리에 섞지 않습니다.  
- 기존 노트북(`dacon/*/*.ipynb`)은 참고용으로 유지하고, 상단에 목적, 데이터 버전, 목표 지표를 짧게 적은 헤더 셀을 추가하세요.  
- 재사용 가능한 유틸은 해당 폴더 내 가벼운 파이썬 모듈로 분리해 셀 복제를 줄입니다.

## 빌드·테스트·개발 명령
- 작업 전 가상환경을 생성/활성화: `python3 -m venv venv`, `source venv/bin/activate`.  
- 의존성 정리는 노트북 완료 후 `pip freeze > requirements.txt`, 공유 시 `pip install -r requirements.txt`.  
- 상대 경로를 유지하려면 루트에서 노트북 실행: `jupyter lab` 또는 `jupyter notebook`.  
- 결과 내보낼 때는 생성물을 `result/`에 두고, 어느 노트북에서 만들었는지 README 셀에 기록합니다.

## 코딩 스타일 및 네이밍
- Python: 스페이스 4칸, 변수/함수는 `snake_case`, 클래스는 `PascalCase`, 폴더·파일은 소문자와 하이픈/언더스코어 사용(예: `comovement_forecast`).  
- 노트북은 가볍게 유지: 긴 실행 기록은 남기지 말고 커밋 전 출력 정리, 큰 순차 셀보다 함수화 선호.  
- 데이터 경로와 주요 파라미터는 노트북 상단에 모아두고, 절대 경로 하드코딩은 피합니다.

## 테스트 가이드
- 중앙 테스트 스위트는 없습니다. 공유 유틸을 추가할 때 `tests/` 폴더와 `pytest` 기반 단위 테스트를 함께 만드세요.  
- 데이터 의존성이 큰 코드는 작은 합성/샘플 데이터를 써서 빠르게 돌리며, 설치 후 루트에서 `pytest` 실행.  
- 의존성 변경 뒤에는 노트북 전체 실행으로 검증하고, 랜덤 시드나 셔플처럼 비결정적 단계는 주석으로 명시합니다.

## 커밋 및 PR 가이드
- Git 기록은 이모지 접두사와 간결한 메시지를 선호합니다(예: `:sparkles: new_experiment`); 명령형으로 유지하세요.  
- 하나의 논리 단위(신규 노트북, 데이터셋 갱신, 리팩터)로 커밋하고, 관련 지표나 결과를 본문에 짧게 남깁니다.  
- PR에는 수정한 노트북, 필요한 데이터셋, 재현 명령, `result/`에 추가한 결과 파일을 적고, 관련 이슈나 대회 스레드가 있으면 링크합니다.

## 데이터 및 보안
- 민감하거나 대용량 원본 데이터는 커밋하지 말고 다운로드 방법이나 `.gitignore`를 활용하세요.  
- 자격 증명과 API 키는 노트북에 넣지 말고 환경 변수나 추적되지 않는 로컬 `.env`에서 불러오세요.
